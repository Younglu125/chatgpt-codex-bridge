# 历史清理后：另一台 Mac 的安全更新

2026-09-20 已授权清理旧提交的私人作者/提交者信息；源码文件树未因历史改写而变化。
`main` 与 `v0.2.9` 的对象 ID 已改变。这一次不能直接按日常 `pull --ff-only` 更新，
也不能把旧分支合并回新历史。完成迁移后恢复日常 fast-forward 更新方式。

给另一台 Mac 的操作提示词：

> 请安全迁移本机 CCB 到 GitHub 的清理后历史，再检查本机安装。仓库是
> https://github.com/Younglu125/chatgpt-codex-bridge 。先定位实际 Git 工作目录，检查
> remote、分支、未提交/未跟踪文件、本机独有提交及旧标签；不得从迁移前的同步副本安装。
> 保留所有修改及必要的仅本机备份，不打印凭据、账号信息或私人邮箱。
> 这次远端已改写历史，不要直接 pull、merge 旧分支、强制推送或 reset --hard。
> 优先在旁边临时目录全新 clone 清理后的远端；辨识本机独有的实际改动，只迁移这些
> 修改，不重放已经存在的旧提交。遇到冲突保留双方内容并正常解决，不能覆盖。
> 检查通过后保留旧目录为仅本机备份，再安全切换到原工作路径，避免破坏项目绑定。
> 不复制另一台机器的凭据或运行状态，不删除本机连接器授权。
> 配置此仓库的 Git 作者和提交者为 GitHub noreply 身份（不要修改全局 Git 配置）；
> 当前维护者公开身份为 Younglu125 / 32756126+Younglu125@users.noreply.github.com。
> 运行隐私扫描（含历史）、Python 测试及适用的上游测试，检查 Python 3.12+ 和 Full 条件，
> 执行 python3 scripts/install_plugin.py 保留已有模式，并新开 Codex 任务验证加载版本。
> 普通 ChatGPT 使用聊天模式，不是 Work。真实发送/读取成功才能报告消息链路通过；
> Full 必须在本机当前项目通过连接器读到正确工作区文件才算连通。缺授权时请我操作。
> 若有必要的代码优化，先测试，再用隐私身份提交并正常推送；不要推送旧历史、备份或状态。
> 最后报告保留的修改、版本、测试和 Lite/Full 实测状态。旧备份仍可能含私人历史，
> 不上传；只有我确认不再需要时再删除。以后常规更新恢复检查改动、git pull --ff-only、
> python3 scripts/install_plugin.py、新开任务的顺序。
