# 使用流程

触发：新会话默认本地 Codex。用户有意请求 `CCB`、`ccb`、`$CCB` 或 `$ccb` 时开启会话协作，后续继承直到明确退出；引用、否定和维护插件不算触发。按实际 Codex 会话 ID 调用 session.py 保存/恢复状态。阶段由 Codex 判断：重要分析、决策、复核按收益与等待成本选择 GPT，日常编辑和测试直接完成；明确要求 GPT 时本次必须调用。

首次：安装插件 → 新任务加载 → 在当前项目绑定自己的普通 ChatGPT 聊天 → 做一次发送/收回测试。

日常：识别当前 checkout → 检查并优先复用该项目已有 Full → 发送唯一任务 → 保存真实分析 → 检查证据变化 → Codex 实施和测试 → 按需交给 ChatGPT 独立审查。

消息默认走原生会话；工具缺失时可走已登录浏览器。原生已确认完成的回合无需依赖模型回显结束标记。
仓库级分析优先复用 Full 实时只读 MCP；小范围已知文件在 Full 不可用时自动走冻结快照，不让用户重复配置。

同一聊天同时只派一个任务。等待超时保留任务编号，下一次通过 pending 继续，不重新发送。
账号或会话变化时重新核验；项目以当前 Codex checkout 为准，MCP 与实施使用同一路径。
完整操作说明集中在 skills/chatgpt-codex-bridge/SKILL.md 及其 references/，无需重复维护第二套流程。
